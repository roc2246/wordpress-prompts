# Explanation Guide

For deep explanations, cover: inputs and outputs; execution order; hooks/callbacks; data sources and transformations; WordPress APIs; template/component relationships; side effects; trust boundaries; frontend behavior; and why the implementation is structured this way. Distinguish what the code definitely does from assumptions that require more project context.
