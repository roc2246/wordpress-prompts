# Exhaustive PHP Review

For full audits, track every inventoried PHP file. Check syntax/runtime risks, hooks, APIs, templates, escaping, input handling, auth/capabilities/nonces, queries, enqueueing, custom content types, ACF/meta handling, naming, duplication, dead code, and maintainability. Review in batches.
