---
name: wordpress-component
description: Create or refactor a reusable WordPress theme component or template part with directly related PHP, SCSS, and JavaScript. Use for component extraction, construction, or standardization.
metadata:
  author: riley-childs
  version: "3.0"
---

# WordPress Component

Use `_base/common-workflow.md` and `_base/common-output.md`.

1. Inspect the nearest existing component/template pattern; inventory only if discovery spans files.
2. Load `references/component-design.md` for structure/API decisions.
3. Load `references/markup-security.md` only for dynamic data or interactive markup.
4. Match existing conventions and implement only the files needed by the component.
