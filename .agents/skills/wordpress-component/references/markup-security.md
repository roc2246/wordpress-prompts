# Markup, Security, Accessibility

- Escape dynamic output according to context.
- Sanitize/validate component inputs at the appropriate boundary; do not double-sanitize trusted internal values unnecessarily.
- Use semantic HTML first.
- Ensure controls have accessible names, keyboard behavior, focus visibility, and correct button/link semantics.
- Add ARIA only when native semantics are insufficient.
